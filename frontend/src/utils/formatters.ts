import type { FindingType, Severity } from '../types';\n\nexport function formatDate(value?: string | null) {\n  if (!value) return 'Not available';\n  return new Intl.DateTimeFormat(undefined, { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(value));\n}\n\nexport function titleCase(value: string) {\n  return value.replace(/_/g, ' ').replace(/\b\w/g, (c) => c.toUpperCase());\n}\n\nexport function severityRank(severity: Severity) {\n  return { critical: 5, high: 4, medium: 3, low: 2, info: 1 }[severity];\n}\n\nexport function severityClasses(severity: Severity) {\n  return { critical: 'bg-red-500/20 text-red-200 ring-red-400/40', high: 'bg-orange-500/20 text-orange-200 ring-orange-400/40', medium: 'bg-amber-500/20 text-amber-100 ring-amber-400/40', low: 'bg-sky-500/20 text-sky-100 ring-sky-400/40', info: 'bg-slate-500/20 text-slate-200 ring-slate-400/40' }[severity];\n}\n\nexport function typeClasses(type: FindingType) {\n  return { open_redirect: 'bg-fuchsia-500/15 text-fuchsia-100', callback_abuse: 'bg-violet-500/15 text-violet-100', token_leak: 'bg-rose-500/15 text-rose-100', mixed_domain_chain: 'bg-cyan-500/15 text-cyan-100' }[type];\n}\n