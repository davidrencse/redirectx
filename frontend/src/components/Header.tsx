import { API_BASE_URL } from '../api/client';\n\nexport default function Header({ route, navigate }: { route: string; navigate: (path: string) => void }) {\n  const link = (path: string, label: string) => <button onClick={() => navigate(path)} className={`rounded-lg px-3 py-2 text-sm font-medium ${route === path ? 'bg-cyan-400 text-slate-950' : 'text-slate-300 hover:bg-white/10'}`}>{label}</button>;\n  return <header className='sticky top-0 z-20 border-b border-white/10 bg-slate-950/90 backdrop-blur'>\n    <div className='mx-auto flex max-w-7xl flex-col gap-3 px-4 py-4 sm:flex-row sm:items-center sm:justify-between'>\n      <button onClick={() => navigate('/')} className='text-left'>\n        <div className='text-xl font-black tracking-tight text-white'>RedirectX</div>\n        <div className='text-xs text-slate-400'>API: {API_BASE_URL}</div>\n      </button>\n      <nav className='flex flex-wrap gap-2'>\n        {link('/', 'Home')}\n        {link('/scan/new', 'New scan')}\n        {link('/demo', 'Demo')}\n        {link('/settings', 'Settings')}\n      </nav>\n    </div>\n  </header>;\n}\n